extends base.pug

block content
    div
        p=video.description
        ul
            each hashtag in video.hashtags
                li=hashtag 
        small=video.createdAt
    a(href=`${video.id}/edit`) Edit Video